{
    "name": "spice-site",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "node ./webapp/bin/www",
        "build": "npm run --prefix webapp/client build",
        "start-react": "npm run --prefix webapp/client start"
    }
}
